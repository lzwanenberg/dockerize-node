version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:2024.05
    steps:
      - checkout
      - run:
          name: Test
          command: |
            TAG=0.1.$CIRCLE_BUILD_NUM
            docker build -t luczwanenberg/001-dockerize-nodejs:$TAG . --target=test

workflows:
  build_and_test:
    jobs:
      - test
